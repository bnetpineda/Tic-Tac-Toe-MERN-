{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "builds": [
      {
          "src": "api/index.js",
          "use": "@vercel/node"
      }
  ],
  "installCommand": "npm install --prefix ./backend && npm install --prefix ./frontend"
}
